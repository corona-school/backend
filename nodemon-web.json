{
	"restartable": "rs",
  "ignore": ["**/*.test.ts", "**/*.spec.ts", "node_modules", ".git", ".devcontainer", ".github", "built", "logs", "integration-tests"],
	"verbose": true,
  "exec": "npm run build && npm run web:dev --",
	"env": {
		"NODE_OPTIONS": "--trace-deprecation"
	},
  "ext": "ts"
}
