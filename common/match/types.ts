export interface MatchChatMetrics {
    pupil: {
        firstMessageSentAt: string;
        lastMessageSentAt: string;
    };
    student: {
        firstMessageSentAt: string;
        lastMessageSentAt: string;
    };
}
