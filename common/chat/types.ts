export enum ChatType {
    COURSE = 'course',
    ANNOUNCEMENT = 'announcement',
}

export enum ContactReason {
    COURSE = 'course',
    MATCH = 'match',
    ANNOUNCEMENT = 'announcement',
    PARTICIPANT = 'participant',
    PROSPECT = 'prospect',
}
